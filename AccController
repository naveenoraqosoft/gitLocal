public with sharing class AccountController {
    
    public static List<Account> getAllAccounts() {
        sytem.debug('inside getAllAccounts');
        return [SELECT Id, Name, AccountNumber, Type, Industry FROM Account limit 100];
    }

}
